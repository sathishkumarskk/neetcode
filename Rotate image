n=eval(input("enter list :"))
l=len(n)
list=[]
for i in range(0,l):
    for j in range(l-1,-1,-1):
        l1=n[j]
        l2=l1[i]
        list.append(l2)
main_list=[list[i:i+l] for i in range(0,len(list),l)]
print(main_list)
