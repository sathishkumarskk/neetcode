matrix=eval(input("enter :))
le=len(matrix[1])
l=[]
mat=[]
for j in range(0,le):
    v=[i[j] for i in matrix]
    l.append(v)
for i in l:
    i.reverse()
    mat.append(i)
print(mat)    
    
    
  
    
                         
